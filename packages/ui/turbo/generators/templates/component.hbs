import React from 'react';

interface {{ pascalCase title }}Props {
    children: React.ReactNode;
}

export const {{ pascalCase title }}: React.FC<{{ pascalCase title }}Props> = ({ children }) => {
    return (
        <div>
            <h1>{{ pascalCase title }} Component</h1>

            {children}
        </div>
    );
};
